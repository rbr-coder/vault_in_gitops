apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secretName }}
  annotations:
    avp.kubernetes.io/path: "kv/secrets_webserver/printing_secret"
type: Opaque
stringData:
  {{ .Values.secretKey }}: "supersecretvalue"